name: Release

on:
  push:
    tags: ["v*"]

permissions: {}

jobs:
  release:
    uses: HeliosBBS/.github/.github/workflows/release.yml@main
    with:
      binary: hadv
      package: ./cmd/hadv
    permissions:
      contents: write
      id-token: write
      attestations: write
